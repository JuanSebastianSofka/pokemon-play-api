# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET /api/pokemon/:id infraestructura.controladores.ControladorPokemon.obtenerPokemon(id: String)
GET /api/pokemones infraestructura.controladores.ControladorPokemon.obtenerTodosLosPokemon

POST /api/crear-pokemon infraestructura.controladores.ControladorPokemon.crearPokemon

DELETE /api/eliminar-pokemon/:id infraestructura.controladores.ControladorPokemon.eliminarPoke(id: String)

PUT /api/actualizar-pokemon infraestructura.controladores.ControladorPokemon.actualizarPoke


